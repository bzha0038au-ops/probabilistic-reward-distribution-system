# Frontend (Planned React Client)

Suggested MVP pages:
- Login/Register
- Wallet overview + transaction history
- Draw page (draw button + result panel)
- Admin panel (prize management + stats)

Suggested API integration:
- Axios + bearer token from `/api/auth/login`
- Unified error interceptor for 401/422/500
